name: wasmJs Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Set up Rust
        run: |
          rustup target add wasm32-unknown-unknown
          echo "RUSTFLAGS=--cfg=web_sys_unstable_apis" >> $GITHUB_ENV

      - name: Install wasm-bindgen CLI
        run: |
          VERSION=$(awk '/^name = "wasm-bindgen"$/{found=1} found && /^version = /{gsub(/"/, "", $3); print $3; exit}' rust/Cargo.lock)
          echo "Installing wasm-bindgen-cli $VERSION to match rust/Cargo.lock"
          cargo install wasm-bindgen-cli --version "$VERSION" --locked

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v6

      - name: Build web distribution
        run: ./gradlew :webApp:wasmJsBrowserDistribution --no-daemon --stacktrace

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: webApp/build/dist/wasmJs/productionExecutable

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
