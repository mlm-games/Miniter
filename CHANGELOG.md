## v0.6.1

- Add image support via image crate
- save unified demuxer
- test oxideav
- Add transition out for clips, and also fix few transitions not appearing
- fix mp4 decode skipping frames
- the logic works and runs on web!
- web shenanigans (vid doesn't play, but projectl loads)
- remove thumbnail caching, causes too many issues to preserve (due to wasm)
- fix kotlin
- platform media paths
- going experimental (use my hardware d&e crate (web init too)
- Create rust-toolchain.toml
- replace encoder with less-avc
- Partial hard subs (save, but add soft subs via ass -> srt in muxide fork)
- replace opusic-sys with mousiki (pure rust)
- save
- Fix dual audio clips not being condensed, and also av1 video being not exported correctly with blank space
- Fix video size issue for few mp4s, and opus audio err
- s1
- add text fade out
- base rust functionality with AOL
- add base rust structure
- Clarify Android save workaround in README


## v0.5.15




## v0.5.14




## v0.5.13




## v0.5.12




## v0.5.11

- remove internet perm
- add screenshots and a note in readme


## v0.5.10

- add screenshots and a note in readme


## v0.5.9




## v0.5.8

- fix clipping with speed filters, and other misc fixes
- icons
- hide text on android (font issues)
- fix text bg not being removable
- text transition testing
- Text refactoring 1
- Better desktop transition UX


## v0.5.7

- build parts
- s3
- cont.
- fix build
- transitions work on desktop only, even text
- multi layer overlay test (jvm works, android partial)
- Fix android saving once again!
- add all the boilerplate rel. stuff
- icon
- Transitions, fill blank space with black bg, and fix project load
- Multi track clipping fix for android
- Fix filters for android
- add error track printing
- File saving logic fix
- android URI handling 1
- fix android crash
- Text rendering, and audio export on jvm
- It exports tracks with filters!
- Add import button back
- Much better timeline sync (although it needs to be paused in between)
- add some logging
- add supported format, auto save wiring, android fixes, and others
- Some more UI changes to wire later
- Export screen wiring (UI only)
- It works when video is paused and changes are made (trial 3 for video preview sync)
- trial 2 for video preview sync
- trial 1 for video preview sync
- refactor for mobile first UI
- add panel before refactor
- Add partial project dialog
- Add import button for more files, for a project, and basic audio wiring
- moving b/w tracks works
- Make preview speed actually go in the right direction
- fix clip snaps, and other minor issues
- Undo redo, opacity, and recent projects wiring
- Add icon
- Better timelines
- Text displays somewhat, mediaplayer doesn't react to few changes inbetween
- android impl + setup extends (like )Uuid
- jvm plays, rest -> placeholders
- save copy


## v0.5.6

- s3
- cont.
- fix build
- transitions work on desktop only, even text
- multi layer overlay test (jvm works, android partial)
- Fix android saving once again!
- add all the boilerplate rel. stuff
- icon
- Transitions, fill blank space with black bg, and fix project load
- Multi track clipping fix for android
- Fix filters for android
- add error track printing
- File saving logic fix
- android URI handling 1
- fix android crash
- Text rendering, and audio export on jvm
- It exports tracks with filters!
- Add import button back
- Much better timeline sync (although it needs to be paused in between)
- add some logging
- add supported format, auto save wiring, android fixes, and others
- Some more UI changes to wire later
- Export screen wiring (UI only)
- It works when video is paused and changes are made (trial 3 for video preview sync)
- trial 2 for video preview sync
- trial 1 for video preview sync
- refactor for mobile first UI
- add panel before refactor
- Add partial project dialog
- Add import button for more files, for a project, and basic audio wiring
- moving b/w tracks works
- Make preview speed actually go in the right direction
- fix clip snaps, and other minor issues
- Undo redo, opacity, and recent projects wiring
- Add icon
- Better timelines
- Text displays somewhat, mediaplayer doesn't react to few changes inbetween
- android impl + setup extends (like )Uuid
- jvm plays, rest -> placeholders
- save copy


## v0.5.5

- fix build
- transitions work on desktop only, even text
- multi layer overlay test (jvm works, android partial)
- Fix android saving once again!
- add all the boilerplate rel. stuff
- icon
- Transitions, fill blank space with black bg, and fix project load
- Multi track clipping fix for android
- Fix filters for android
- add error track printing
- File saving logic fix
- android URI handling 1
- fix android crash
- Text rendering, and audio export on jvm
- It exports tracks with filters!
- Add import button back
- Much better timeline sync (although it needs to be paused in between)
- add some logging
- add supported format, auto save wiring, android fixes, and others
- Some more UI changes to wire later
- Export screen wiring (UI only)
- It works when video is paused and changes are made (trial 3 for video preview sync)
- trial 2 for video preview sync
- trial 1 for video preview sync
- refactor for mobile first UI
- add panel before refactor
- Add partial project dialog
- Add import button for more files, for a project, and basic audio wiring
- moving b/w tracks works
- Make preview speed actually go in the right direction
- fix clip snaps, and other minor issues
- Undo redo, opacity, and recent projects wiring
- Add icon
- Better timelines
- Text displays somewhat, mediaplayer doesn't react to few changes inbetween
- android impl + setup extends (like )Uuid
- jvm plays, rest -> placeholders
- save copy


## v0.5.4

- transitions work on desktop only, even text
- multi layer overlay test (jvm works, android partial)
- Fix android saving once again!
- add all the boilerplate rel. stuff
- icon
- Transitions, fill blank space with black bg, and fix project load
- Multi track clipping fix for android
- Fix filters for android
- add error track printing
- File saving logic fix
- android URI handling 1
- fix android crash
- Text rendering, and audio export on jvm
- It exports tracks with filters!
- Add import button back
- Much better timeline sync (although it needs to be paused in between)
- add some logging
- add supported format, auto save wiring, android fixes, and others
- Some more UI changes to wire later
- Export screen wiring (UI only)
- It works when video is paused and changes are made (trial 3 for video preview sync)
- trial 2 for video preview sync
- trial 1 for video preview sync
- refactor for mobile first UI
- add panel before refactor
- Add partial project dialog
- Add import button for more files, for a project, and basic audio wiring
- moving b/w tracks works
- Make preview speed actually go in the right direction
- fix clip snaps, and other minor issues
- Undo redo, opacity, and recent projects wiring
- Add icon
- Better timelines
- Text displays somewhat, mediaplayer doesn't react to few changes inbetween
- android impl + setup extends (like )Uuid
- jvm plays, rest -> placeholders
- save copy


## v0.5.3

- transitions work on desktop only, even text
- multi layer overlay test (jvm works, android partial)
- Fix android saving once again!
- add all the boilerplate rel. stuff
- icon
- Transitions, fill blank space with black bg, and fix project load
- Multi track clipping fix for android
- Fix filters for android
- add error track printing
- File saving logic fix
- android URI handling 1
- fix android crash
- Text rendering, and audio export on jvm
- It exports tracks with filters!
- Add import button back
- Much better timeline sync (although it needs to be paused in between)
- add some logging
- add supported format, auto save wiring, android fixes, and others
- Some more UI changes to wire later
- Export screen wiring (UI only)
- It works when video is paused and changes are made (trial 3 for video preview sync)
- trial 2 for video preview sync
- trial 1 for video preview sync
- refactor for mobile first UI
- add panel before refactor
- Add partial project dialog
- Add import button for more files, for a project, and basic audio wiring
- moving b/w tracks works
- Make preview speed actually go in the right direction
- fix clip snaps, and other minor issues
- Undo redo, opacity, and recent projects wiring
- Add icon
- Better timelines
- Text displays somewhat, mediaplayer doesn't react to few changes inbetween
- android impl + setup extends (like )Uuid
- jvm plays, rest -> placeholders
- save copy


# Changelog

## v0.5.2

- transitions work on desktop only, even text
- multi layer overlay test (jvm works, android partial)
- Fix android saving once again!
- add all the boilerplate rel. stuff
- icon
- Transitions, fill blank space with black bg, and fix project load
- Multi track clipping fix for android
- Fix filters for android
- add error track printing
- File saving logic fix
- android URI handling 1
- fix android crash
- Text rendering, and audio export on jvm
- It exports tracks with filters!
- Add import button back
- Much better timeline sync (although it needs to be paused in between)
- add some logging
- add supported format, auto save wiring, android fixes, and others
- Some more UI changes to wire later
- Export screen wiring (UI only)
- It works when video is paused and changes are made (trial 3 for video preview sync)
- trial 2 for video preview sync
- trial 1 for video preview sync
- refactor for mobile first UI
- add panel before refactor
- Add partial project dialog
- Add import button for more files, for a project, and basic audio wiring
- moving b/w tracks works
- Make preview speed actually go in the right direction
- fix clip snaps, and other minor issues
- Undo redo, opacity, and recent projects wiring
- Add icon
- Better timelines
- Text displays somewhat, mediaplayer doesn't react to few changes inbetween
- android impl + setup extends (like )Uuid
- jvm plays, rest -> placeholders
- save copy

