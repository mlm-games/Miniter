Miniter is a basic Compose Multiplatform Video Editor mainly for Android, and Linux.


Is limited by ffmpeg android support, hence planning to instead focus on a rust based editor directly for linux (and android later, basic tasks are covered here), so this is basically a prototype that works well for simple tasks, like trimming, and combining multiple tracks (on android), while transitions and text overlays work better on desktop. 
